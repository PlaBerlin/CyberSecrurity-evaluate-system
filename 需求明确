1.评分页面，数据集是什么？
一个批次的数据是一个数据集，爬虫每自动采集一次 对应一个批次

2.关于国际组织统计报告的获取，是直接放在自动采集爬虫 数据页面自动采集处 采集到吗？那么不仅要将爬取到的文本按指标分类，还要将其中的统计报告提取出来？

3.关于采集数据的存储问题。

爬取到的文本文本和对应的翻译文本存储在本地，
特定字段，采集时间、标题、来源、所属指标等元数据存储在数据库中，需要一个特定字段记录文本在本地的存储地址


4.关键信息抽取这部分，抽取到的是结构化的元数据，具体字段如下：
采集时间、标题、来源、所属指标，人名、地名、机构名、职位名称，资料的发布时间、发布国家或机构、资料中涉及到的国家（为后期做进一步关联信息提供支持）、人物、主要事件概括
可能还有别的信息，需要和甲方进一步沟通

5.该国家该指标对应多个评分依据页面，点击小球后进入已采集数据列表页面，对其进行审核。
